# Override the Apache User (vhosts are included at the end of apache2.conf)
User vagrant
Group vagrant

<VirtualHost *:80>
  Servername presence.lo
  ServerAlias presence.example.com
  DocumentRoot /vagrant/web
  <Directory "/vagrant/web">
    AllowOverride All
    Require all granted
  </Directory>
</VirtualHost>

